# Dependências do projeto TP01
# Instale com: pip install -r requirements.txt

nats-py    # biblioteca para conectar ao NATS (asyncio-based client)
flask      # framework web para servir o dashboard de monitoramento
requests   # para consultar a API HTTP de monitoramento do NATS
matplotlib # para gerar gráficos de benchmark em PNG
